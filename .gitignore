# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Environments and secrets
.env
# Local MCP wiring: only the .example ships. A real .mcp.json points at a
# local venv path and must never carry credentials anyway (env-only rule).
.mcp.json

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime outputs of the reference agent (receipts live with the operator,
# not in the repo)
audit-log.jsonl
approvals/
# Trusted-path enrollment: holds only an scrypt hash + salt, but it is
# operator-local credential material all the same. Never committed.
approval-secret.json

# Machine-local Claude context (see CLAUDE.md)
CLAUDE.local.md
